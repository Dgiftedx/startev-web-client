import { StSelectDirective } from './st-select.directive';

describe('StSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new StSelectDirective();
    expect(directive).toBeTruthy();
  });
});
